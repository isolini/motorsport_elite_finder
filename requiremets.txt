# =========================
# Projeto Telemetria IA — requirements.txt (inicial)
# Python 3.11+
# =========================

# --- Core API/serviços ---
fastapi==0.115.*           # API (HTTP) e WebSocket
uvicorn[standard]==0.30.*  # servidor ASGI (inclui uvloop, websockets, etc.)

# --- Análise de dados ---
pandas==2.2.*              # manipulação de dados
numpy==1.26.*              # base numérica

# --- Configuração/validação ---
pydantic==2.9.*            # modelos (schemas)
pydantic-settings==2.5.*   # leitura de .env e variáveis de ambiente

# --- Integrações ---
httpx==0.27.*              # cliente HTTP (chamada ao LLM/DeepSeek)
websockets==12.*           # cliente WS p/ scripts de simulação (evita depender só do transitive)

# --- Persistência---
# SQLAlchemy + SQLite assíncrono (bom para MVP local):
sqlalchemy==2.0.*
aiosqlite==0.20.*

# --- Dev/Qualidade ---
pytest==8.*                # testes
# ruff==0.6.*              # lint/format rápido (opcional)
# mypy==1.11.*             # type-checking (opcional)

# ---  TTS ---
# coqui-tts==0.22.*        # alternativa open-source, piper tem instalação local